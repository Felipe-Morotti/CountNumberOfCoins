# CountNumberOfCoins

This is my first ever ML project, a very simple one that uses OpenCv for image processing.
It consists of processing an image so the program can say how many coins are in the image.